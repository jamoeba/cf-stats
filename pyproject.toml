[build-system]
requires = [
    "setuptools>=45",
]
build-backend = "setuptools.build_meta"

[project]
name = "cf-stats"
version = "0.1.0"
description = "cf stats tool"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]
